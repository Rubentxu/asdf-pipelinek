#!/usr/bin/env bash
# asdf-pipelinek bin/list-bin: expose launcher path to shims; fail closed if absent.
set -euo pipefail

VERSION="${ASDF_INSTALL_VERSION}"
install_dir="${ASDF_INSTALL_PATH}"

if [ -x "${install_dir}/bin/pipelinek" ]; then
  echo "bin"
else
  exit 1
fi
